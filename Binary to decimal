#include<bits/stdc++.h>
using namespace std;
int main(){
    string str;
    cout<<"enter a string";
    cin>>str;
    int sum=0;
    int p2=1;
    for(int i=str.length()-1;i>=0;i--){
        if(str[i]=='1'){
            sum=sum+p2;
        }
        p2=p2*2;
    }
    cout<<sum;
}
