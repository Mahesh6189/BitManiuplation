#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,i;
    cout<<"enter a number";
    cin>>n;
    cout<<"enter the bit numbers which have to check";
    cin>>i;
    if((n>>(i-1))&1!=0){
        cout<<"bit is set";
    }
    else{
        cout<<"bit is not set";
    }
    
}
